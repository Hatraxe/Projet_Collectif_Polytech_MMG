The "venv" folder in the gitignore (in the case of a Windows installation) contains all the python libraries needed to work on the project.
It's probably mandatory to launch the project, so it's advisable to have it generated by QT Creator first (it weighs more than 500MB, so it goes in the gitignore).